<small><%= post.id %>: <%= post.username %> (<a href="/<%= post.id %>" title="Show post"><%= post.created_at.toLocaleString() %></a>)</small>
<p><%= post.message %></p>

<% if (user && user.role === "admin") { %>
    <form method="POST" action="/<%= post.id %>?_method=DELETE"">
	<button type="submit">Delete post</button>
    </form>
<% } %>
